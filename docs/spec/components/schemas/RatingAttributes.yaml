type: object
required:
  - reviewId
  - userId
  - rating
properties:
  reviewId:
    type: integer
    format: int64
  userId:
    type: integer
    format: int64
  rating:
    type: integer
    format: int64
