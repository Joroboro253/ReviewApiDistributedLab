type: object
required:
  - reviewId
  - includeRatings
  - sortBy
  - page
  - limit
  - sortDirection
properties:
  reviewId:
    type: integer
    format: int64
  includeRatings:
    type: boolean
    format: bool
  sortBy:
    type: string
  page:
    type: integer
    format: int64
  limit:
    type: integer
    format: int64
  sortDirection:
    type: string