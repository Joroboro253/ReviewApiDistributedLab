type: object
properties:
  sortBy:
    type: string
  page:
    type: integer
    format: int64
  limit:
    type: integer
    format: int64
  sortDirection:
    type: string
required:
  - sortBy
  - page
  - limit
  - sortDirection