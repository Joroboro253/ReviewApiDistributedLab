type: object
required:
  - limit
  - page
  - sortBy
  - sortDirection
properties:
  limit:
    type: integer
    format: int64
  page:
    type: integer
    format: int64
  sortBy:
    type: string
  sortDirection:
    type: string