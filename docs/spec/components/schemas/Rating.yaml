type: object
properties:
  id:
    type: integer
    format: int64
  reviewID:
    type: integer
    format: int64
  userID:
    type: integer
    format: int64
  rating:
    type: number
    format: float64
  createdAt:
    type: string
    format: time.Time
    nullable: true
  updatedAt:
    type: string
    format: time.Time
    nullable: true
required:
    - id
    - reviewID
    - userID
    - rating
    - createdAt
    - updatedAt