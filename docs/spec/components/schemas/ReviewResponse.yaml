components:
  schemas:
    ReviewResponse:
      type: object
      properties:
        Data:
          $ref: '#/components/schemas/Review'
    Review:
      type: object
      properties:
        ID:
          type: integer
          format: int64
        ProductID:
          type: integer
          format: int64
        UserID:
          type: integer
          format: int64
        Content:
          type: string